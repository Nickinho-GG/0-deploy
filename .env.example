# MongoDB Atlas - Connection String Configuration
# 
# Copie este arquivo para .env (apenas para desenvolvimento local)
# Na Vercel, configure esta variável em: Settings > Environment Variables

# Connection String do MongoDB Atlas
# Formato: mongodb+srv://usuario:senha@cluster.xxxxx.mongodb.net/?appName=ClusterName
MONGODB_URI=mongodb+srv://usuario:senha@cluster0.xxxxx.mongodb.net/?appName=Cluster0

# Observações:
# - Substitua "usuario" pelo seu username do MongoDB
# - Substitua "senha" pela sua senha do MongoDB
# - Substitua "cluster0.xxxxx" pelo seu cluster real
# - O banco de dados usado será "logindb" (configurado no código)
# - A coleção será "usuarios" (configurado no código)
